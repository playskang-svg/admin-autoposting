services:
  - type: web
    name: admin-autoposting
    env: node
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 20
      - key: GEMINI_API_KEY
        sync: false
# redeploy trigger: 2026-09-15T10:50
